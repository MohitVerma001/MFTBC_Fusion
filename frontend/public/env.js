/**
 * Runtime Environment Configuration
 * This file is loaded at runtime and allows environment variables to be changed without rebuilding
 * FUSO Brand Theme: Never use Supabase - PostgreSQL + Express backend only
 */
window.__ENV = {
  VITE_API_BASE_URL: 'http://localhost:5000'
};
